/**
 * Maximum number of links a user may pin to the sidebar.
 * Single source of truth for the cap; mirrored by the backend
 * validation rule in UpdateSidebarPinnedLinksRequest (max:5).
 */
export const MAX_SIDEBAR_PINNED_LINKS = 5;
