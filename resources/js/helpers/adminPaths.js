/** SPA path for branch selection (must match admin.js route path). */
export const ADMIN_BRANCH_SELECT_PATH = '/admin/select-branch';

/** SPA path for the "module not enabled" screen (must match admin.js route path). */
export const ADMIN_MODULE_UNAVAILABLE_PATH = '/admin/module-unavailable';
